apiVersion: v1
kind: Service
metadata:
  name: tradewise-prometheus
spec:
  selector:
    app: tradewise-prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090
  type: NodePort
