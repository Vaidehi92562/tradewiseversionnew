apiVersion: v1
kind: Service
metadata:
  name: tradewise-backend-service
spec:
  selector:
    app: tradewise-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort