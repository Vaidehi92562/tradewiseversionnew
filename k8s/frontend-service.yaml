apiVersion: v1
kind: Service
metadata:
  name: tradewise-frontend-service
spec:
  selector:
    app: tradewise-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort